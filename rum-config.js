// Placeholder served by `vitepress dev` so /docs/rum-config.js doesn't 404.
// In the production nginx container this file is overwritten at pod start by
// envsubst on deployment/docker/rum-config.js.template using values from the
// widget-service-docs ConfigMap. Always a no-op outside k8s.
